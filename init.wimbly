
-- preprocess application configuration files
wimbly.preprocess( '/var/www/lua', {
  ['app'] = '/var/www/lua',
  ['prefix'] = '/v2',
} )

-- during development wrap all content_by_lua_file directives to display errors in browser 
wimbly.debug( '/var/www/lua', 'endpoints%.nginx$' )

